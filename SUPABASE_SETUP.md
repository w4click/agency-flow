# AgencyFlow — Guia de Configuração do Supabase

## Visão Geral

O AgencyFlow agora suporta Supabase como banco de dados oficial para os mwãdulos (tabela `clientes`), ej, Ib 
 clientes (tabela `clientes`)
- **Equipe** (tabela `usuarios`)
- **Permissões** (tabela `permissoes`)
- **Autenticação** via Supabase Auth
